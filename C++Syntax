#include<iostream>

using namespace std;

int main() {
    int a,b,c,d;
    cin >> a >> b >> c >> d; // 用空格隔开 and  输入共四行，第一行包含整数 A，
    //第二行包含整数 B，第三行包含整数 C，第四行包含整数 D。
    cout << "DIFERENCA = " << (a * b) - (c * d) << endl;
}
